name: nyuciin_app
description: Aplikasi NYUCIIN - Laundry Service Platform
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  http: ^0.13.6
  shared_preferences: ^2.1.1
  jwt_decode: ^0.3.1
  geolocator: ^9.0.2
  socket_io_client: ^2.0.0
  flutter_dotenv: ^5.0.2
  cupertino_icons: ^1.0.5
  google_maps_flutter: ^2.2.4
  flutter_svg: ^1.1.6
  intl: ^0.18.0
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/

